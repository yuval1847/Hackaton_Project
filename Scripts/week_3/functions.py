from constants import *
import pygame


# def add_text(screen, text, color, x_pos, y_pos, size):
#     # The function get the screen, the text, the text color, the x and y position and the font size
#     # The function add the text's surface the text's rectangle on the screen
#     font = pygame.font.Font(FONT1, size)
#     text_surface = font.render(text, True, color)
#     text_rect = text_surface.get_rect(center=(x_pos, y_pos))
#     screen.blit(text_surface, text_rect)